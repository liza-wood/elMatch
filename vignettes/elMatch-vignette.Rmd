---
title: "elMatch-vignette"
output: rmarkdown::html_vignette
vignette: >
  %\VignetteIndexEntry{elMatch-vignette}
  %\VignetteEngine{knitr::rmarkdown}
  %\VignetteEncoding{UTF-8}
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(elMatch)
```

Pull the list of projects

Pull in the applicant data

Read in a sample transcript
```{r, eval = F}
r_mat <- pull_research_needs(need url)
s_mat <- pull_student_interest()

proj_list <- identify_top_matches(r_mat, s_mat)
```

